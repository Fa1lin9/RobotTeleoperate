#   Package path config
if( ${USER_TANGTANG_p15v3_Ubuntu} )
    include( package_path_per_user/TANGTANG_p15v3_Ubuntu )
endif()

if( ${USER_TANGTANG_p15v3_Windows} )
    include( package_path_per_user/TANGTANG_p15v3_Windows )
endif()

if( ${USER_TANGTANG_t14s_Ubuntu} )
    include( package_path_per_user/TANGTANG_t14s_Ubuntu )
endif()

if( ${USER_TANGTANG_t14s_Windows} )
    include( package_path_per_user/TANGTANG_t14s_Windows )
endif()

if( ${USER_TANGTANG_3090_Ubuntu} )
    include( package_path_per_user/TANGTANG_3090_Ubuntu )
endif()

if( ${USER_TANGTANG_3090_Windows} )
    include( package_path_per_user/TANGTANG_3090_Windows )
endif()

if( ${USER_TANGTANG_4090_Ubuntu} )
    include( package_path_per_user/TANGTANG_4090_Ubuntu )
endif()

if( ${USER_TANGTANG_4090_Windows} )
    include( package_path_per_user/TANGTANG_4090_Windows )
endif()

# USER_CHENJUSHUAI_Notebook_Ubuntu
if( ${USER_CHENJUSHUAI_Notebook_Ubuntu} )
    include( package_path_per_user/CHENJUSHUAI_Notebook_Ubuntu )
endif()

# USER_CHENJUSHUAI_Desktop_Ubuntu
if( ${USER_CHENJUSHUAI_Desktop_Ubuntu} )
    include( package_path_per_user/CHENJUSHUAI_Desktop_Ubuntu )
endif()

# USER_CHENJUSHUAI_Desktop_Ubuntu
if( ${USER_SHENTIAN_Desktop_Ubuntu} )
    include( package_path_per_user/SHENTIAN_Desktop_Ubuntu )
endif()

# USER_CHENJUSHUAI_Desktop_Ubuntu
if( ${USER_ROBOT_NoteBook_Ubuntu} )
    include( package_path_per_user/ROBOT_NoteBook_Ubuntu )
endif()

# USER_djr_CAD_404_Windows
if( ${USER_djr_CAD_404_Windows} )
    include( package_path_per_user/djr_CAD_404_Windows )
endif()

# USER_djr_lenovo_Windows
if( ${USER_djr_lenovo_Windows} )
    include( package_path_per_user/djr_lenovo_Windows )
endif()
# USER_djr_lenovo_Windows
if( ${USER_djr_lenovo_Ubuntu} )
    include( package_path_per_user/djr_lenovo_Ubuntu )
endif()

# USER_djr_lenovo_Windows
if( ${USER_djr_107_Ubuntu} )
    include( package_path_per_user/djr_107_Ubuntu )
endif()

# USER_djr_lenovo_Windows
if( ${USER_djr_107_Ubuntu_away_from_door} )
    include( package_path_per_user/djr_107_Ubuntu_away_from_door )
endif()

# CRP_Pk_Ubuntu2004
if( ${USER_CRP_Pk_Ubuntu2004} )
    include( package_path_per_user/CRP_Pk_Ubuntu2004 )
endif()

# USER_CHF_Ubuntu
if( ${USER_CHF_Ubuntu} )
    include( package_path_per_user/CHF_Ubuntu )
endif()